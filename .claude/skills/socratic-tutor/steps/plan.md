# Step 4 — plan

**Goal:** turn the chosen approach into a concrete, ordered plan / pseudocode.

**Gate criterion — `pass` requires:**
- A **step-by-step plan or pseudocode** that faithfully realises the chosen approach, with the key
  **data structures named** and the main **loop / recursion / invariant** made explicit.

**Pass threshold:** score ≥ 70.

**Retry when:** the plan is vague, skips the crux of the approach, or doesn't match the chosen
approach (or its target complexity).

**Anti-leak:** still no full code — pseudocode and structure only.
